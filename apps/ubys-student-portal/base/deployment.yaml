apiVersion: apps/v1
kind: Deployment
metadata:
  name: ubys-student-portal
  labels:
    app: ubys-student-portal
spec:
  selector:
    matchLabels:
      app: ubys-student-portal
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: ubys-student-portal
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: ubys-student-portal
          image: harbor.example.com/ubys/student-portal:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: APP_ENV
              valueFrom:
                configMapKeyRef:
                  name: ubys-student-portal-config
                  key: APP_ENV
            - name: API_BASE_URL
              valueFrom:
                configMapKeyRef:
                  name: ubys-student-portal-config
                  key: API_BASE_URL
            - name: APP_SECRET
              valueFrom:
                secretKeyRef:
                  name: ubys-student-portal-secret
                  key: APP_SECRET
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
      imagePullSecrets:
        - name: harbor-registry-secret
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - ubys-student-portal
                topologyKey: kubernetes.io/hostname
