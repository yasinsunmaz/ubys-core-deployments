apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ubys-core-web-app
  namespace: ubys-core-web-app-staging
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.ubys.com
      secretName: ubys-core-web-app-staging-tls
  rules:
    - host: staging.ubys.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ubys-core-web-app
                port:
                  number: 80
