apiVersion: v1
kind: Service
metadata:
  name: ubys-core-web-app
  labels:
    app: ubys-core-web-app
spec:
  selector:
    app: ubys-core-web-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
